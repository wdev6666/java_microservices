server:
  port: 8084

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wdev6666/microservice-tutorial-config
          clone-on-start: true